---
const { header } = Astro.props;
---

<section class="hero">
  <h1 class="hero__heading">{header}</h1>
</section>

<style>
  .hero {
    width: 90%;
    max-width: 1600px;
    margin: 0 auto;
    padding: 2rem 0 0;
  }

  .hero h1 {
    font-family: var(--font);
    font-size: clamp(3rem, 8vw, 4rem);
    font-weight: 700;
    line-height: 1.05;
    margin: 0 0 var(--space-m);
    text-transform: uppercase;
  }
</style>
